import { IconConfig } from '@spartacus/storefront';

export const makaIconConfig: IconConfig = {
  icon: {
    symbols: {
      CARET_LEFT: 'icon-chevron-left',
      CARET_RIGHT: 'icon-chevron-right',
      GRID: 'd-none', // product views component use alternative images
      LIST: 'd-none'
    }
  }
} as IconConfig;
